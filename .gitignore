# Python
__pycache__/
*.pyc
venv/
.venv/

# قواعد البيانات المحلية
*.db
*.sqlite
*.sqlite3

# نسخ احتياطية قديمة
*.bak

# اكواد التفعيل (بيانات حساسة)
activation_codes_*.json
رموز_التفعيل_*

# ملفات بناء فلاتر
dental_app/build/
dental_app/.dart_tool/
dental_app/.flutter-plugins
dental_app/.flutter-plugins-dependencies

# ملفات النظام
.DS_Store
Thumbs.db

# الملفات المرفوعة من المستخدمين (صور اشعة وغيرها)
uploads/

# سجلات انهيار JVM التي يخلّفها Gradle عند فشل البناء (2026-09-05)
hs_err_pid*.log
replay_pid*.log

# مفاتيح Firebase السرّية (Service Account) -- لا تُرفع أبداً
*firebase*adminsdk*.json

# ثغرة في الأنماط أعلاه، سُدَّت 2026-09-19: النمطان القديمان يحجبان
# activation_codes_*.json بالشرطة السفلية وبامتداد json وحده، فمرّ
# activation-codes_2026-09-19_1146.csv (شرطة وسطى + csv) غيرَ محجوب
# وظهر في git status جاهزاً لأي git add -A. أكواد التفعيل لا تُرفع أبداً
# مهما كان فاصلها أو امتدادها.
activation-codes*
activation_codes*

# مخرجات Claude: ملفات عمل مؤقتة، وقد تحمل بيانات حسّاسة كما حدث أعلاه.
Claude outputs/
analyze.txt
