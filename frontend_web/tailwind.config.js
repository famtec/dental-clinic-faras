/** @type {import('tailwindcss').Config} */
// إعداد Tailwind لبناء نسخة إنتاجية (مبنية مسبقاً، بأصناف مُقلَّمة) بدل الاعتماد
// على cdn.tailwindcss.com التطويري في المتصفح مباشرة (2026-08-25). لا يوجد أي
// تخصيص لموضوع Tailwind (ألوان/خطوط/تباعد) في أي صفحة عبر CDN حالياً -- كل
// الصفحات تستخدم القيم الافتراضية + صياغة القيم الحرة [مثل bg-[radial-gradient(...)]]
// التي يدعمها محرك JIT مباشرة بلا أي إعداد إضافي.
module.exports = {
  content: ["./*.html", "./*.js"],
  theme: {
    extend: {},
  },
  plugins: [],
};
